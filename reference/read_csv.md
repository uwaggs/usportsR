# reads a csv from a url with a csv

reads a csv from a url with a csv

## Usage

``` r
read_csv(file_url)
```

## Arguments

- file_url:

  A URL to a CSV file.

## Value

A data frame with the first few rows of the CSV file.
